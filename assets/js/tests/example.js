var chai = require("chai"),
    should = chai.should();

/*

var User = require('../models/user');

describe('User model', function () {
    it('should have default name', function () {
        var u = new User();
        u.get('name').should.equal('Username');
    });
});

*/