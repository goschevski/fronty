/*
define(['models/user'], function (User) {

    describe('User model', function () {
        var user;

        beforeEach(function () {
            user = new User();
        });

        it('should have logged status', function () {
            expect(user.get('logged')).toBe(false);
        });

    });

});
*/